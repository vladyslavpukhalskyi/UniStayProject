namespace Domain.Listings;

public class ListingEnums
{
    public enum ListingType { House, Apartment, Room }
    public enum CommunalService { Included, Separate }
    public enum OwnershipType { With, Without }
    public enum NeighbourType { With, Without }
}